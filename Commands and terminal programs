----Programs----
-------------
---Network---
-Attacks-
ettercap
reaver
arp

-Sniffers-
nmap
netdiscover
ip
p0f
ettercap
nbtscan
dsniff
etherape
wpa_supplicant (may have a rootkit)

-Stealth-
macchanger

-Cracking-
nikto
aircrack-ng
0d1n

-Remote Connections-
ssh
ftp
telnet

-Basic Tools-
iprange
net
less - command line file viewer

---Cryptography---
sha256sum

---------------------------------
---System Administration Tools---

--Terminals--
xterm
mateterminal

--Process Utility--
ps
Stacer
htop

--Package and Repository Management--
dpkg
synaptic
apt
yum
git
get-config
wget
nano

--Keylogger--
logkeys

--Search--
ls
find
--------------
---Security---

--Firewalls--
firehol
comodo
ufw
denyhosts

--Scanners--
chkrootkit
rkhunter
tiger

--Cleaner--
Stacer
BleachedBit

--Analysis--
lynis

--Lockers--
xtrlock
away

--Forensics--
bulk_extractor

--Passwords--
psswd (changes password)

--Directories--
pwd

-------------------
----Commands----
su //login as root user

sudo apt-get autoclean && sudo apt-get autoremove //cleans junk up

rm -r [directory] //removes the specified directory or file

sudo apt-get install [program] //looks for package and installs the program if it exists

sudo add-apt-repository [url] //adds a repository

mate-terminal

./0d1n (runs the 0d1n software)

sudo netstat -tup (show active internet connections)

free -m (shows amount of ram)

who -a (shows who is connected to your pc, including you)


sudo apt-get install chkrootkit &&
sudo apt-get install etherape &&
sudo apt-get install nikto &&
sudo apt-get install ettercap &&
sudo apt-get install rkhunter &&
sudo apt-get install p0f &&
sudo apt-get install nmap &&
sudo apt-get install aircrack-ng &&
sudo apt-get install netdiscover &&
sudo apt-get install reaver &&
sudo apt-get install nbtscan &&
sudo apt-get install macchanger && 
sudo apt-get install synaptic && 
sudo apt-get install firehol && 
sudo apt-get install htop && 
sudo apt-get install git && 
sudo apt-get install get-config && 
sudo apt-get install nano && 
sudo apt-get install apt && 
sudo apt-get install dpkg && 
sudo apt-get install logkeys &&
sudo apt-get install denyhosts && 
sudo apt-get install tiger && 
sudo apt-get install lynis && 
sudo apt-get install away && 
sudo apt-get install xtrlock

echo 'password' | sudo -S gnome-terminal
# Opens gnome terminal then login u in as a root user
